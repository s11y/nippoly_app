<% unless comments.empty? %>
  <ul>
    <% comments.each do |comment| %>
      <li>
        <p><%= link_to comment.user.name, user_path(comment.user) %></p>
        <p><%= comment.body %></p>
      </li>
    <% end %>
  </ul>
<% end %>
<%= render 'comments/new' %>